const CACHE='lockbox-vault-v9';const APP=['/','/manifest.webmanifest','/assets/app-icon.svg'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(APP)));self.skipWaiting()});self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));const networkFirst=async r=>{try{const res=await fetch(r);if(res?.ok&&res.type!=='opaque'){const c=await caches.open(CACHE);c.put(r,res.clone())}return res}catch{return caches.match(r)}};self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;if(e.request.mode==='navigate'||['script','style'].includes(e.request.destination)){e.respondWith(networkFirst(e.request).then(r=>r||caches.match('/')));return}const url=new URL(e.request.url);if(url.origin===self.location.origin)e.respondWith(caches.match(e.request).then(c=>c||networkFirst(e.request)))})